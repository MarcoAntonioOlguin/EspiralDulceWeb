  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- SEO -->
  <meta name="description" content="{{ description }}" />
  <meta name="keywords"    content="{{ keywords }}" />
  <meta name="theme-color" content="#C87941" />
  <link rel="canonical" href="{{ site.url }}{{ canonical }}" />

  <!-- Open Graph -->
  <meta property="og:type"        content="website" />
  <meta property="og:site_name"   content="{{ site.nombre }}" />
  <meta property="og:locale"      content="es_MX" />
  <meta property="og:title"       content="{{ ogTitle }}" />
  <meta property="og:description" content="{{ ogDescription }}" />
  <meta property="og:url"         content="{{ site.url }}{{ canonical }}" />
  <meta property="og:image"       content="{{ site.url }}{{ ogImage }}" />
  <meta property="og:image:alt"   content="{{ ogImageAlt }}" />

  <!-- Twitter -->
  <meta name="twitter:card"        content="summary_large_image" />
  <meta name="twitter:title"       content="{{ ogTitle }}" />
  <meta name="twitter:description" content="{{ ogDescription }}" />
  <meta name="twitter:image"       content="{{ site.url }}{{ ogImage }}" />

  <!-- Favicon & Manifest -->
  <link rel="icon" type="image/svg+xml" href="images/logos/logo_minimalista.svg" />
  <link rel="apple-touch-icon" href="images/logos/icon-180.png" />
  <link rel="manifest" href="manifest.json" />

  <title>{{ title }}</title>

  <!-- Schema.org: Bakery / LocalBusiness -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Bakery",
    "name": "{{ site.nombre }}",
    "description": "Repostería artesanal a pedido. Pasteles, panques, galletas y postres con entrega a domicilio en toda la CDMX.",
    "url": "{{ site.url }}/",
    "image": "{{ site.url }}/images/og-image.png",
    "logo": "{{ site.url }}/images/logos/icon-512.png",
    "telephone": "{{ site.telefono.e164 }}",
    "priceRange": "$$",
    "servesCuisine": ["Repostería", "Postres", "Pastelería artesanal"],
    "address": {
      "@type": "PostalAddress",
      "addressLocality": "{{ site.ubicacion.colonia }}",
      "addressRegion": "{{ site.ubicacion.alcaldia }}, {{ site.ubicacion.ciudad }}",
      "addressCountry": "{{ site.ubicacion.pais }}"
    },
    "areaServed": {
      "@type": "City",
      "name": "Ciudad de México"
    },
    "openingHoursSpecification": [{
      "@type": "OpeningHoursSpecification",
      "dayOfWeek": ["Monday","Tuesday","Wednesday","Thursday","Friday","Saturday"],
      "opens": "09:00",
      "closes": "19:00"
    }],
    "sameAs": [
      "{{ site.redes.instagram }}",
      "{{ site.redes.facebook }}"
    ]
  }
  </script>

  <!-- Google Fonts: Cormorant Garamond (títulos) + DM Sans (cuerpo) -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,600;0,700;1,400;1,600&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet" />

  <!-- Hojas de estilo propias, en orden de cascada: tokens → base → componentes →
       secciones. portafolio.css trae las flip cards (index y portafolio) y
       `pageCss` (opcional) lo declara la página en su front matter. -->
  <link rel="stylesheet" href="css/tokens.css" />
  <link rel="stylesheet" href="css/base.css" />
  <link rel="stylesheet" href="css/componentes.css" />
  <link rel="stylesheet" href="css/secciones.css" />
  <link rel="stylesheet" href="css/portafolio.css" />
  {%- if pageCss %}
  <link rel="stylesheet" href="{{ pageCss }}" />
  {%- endif %}

  {%- if env.GA4_ID %}
  <!-- Google Analytics 4 — el ID viene de GA4_ID en el .env; sin ID no se carga nada -->
  <script async src="https://www.googletagmanager.com/gtag/js?id={{ env.GA4_ID }}"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', '{{ env.GA4_ID }}');
  </script>
  {%- endif %}